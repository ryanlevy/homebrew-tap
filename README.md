# Homebrew Tap for Maxent for OSX
## Install Maxent
`brew tap ryanlevy/tap`  
`brew install maxent`  

If `alpscore` is not installed through homebrew, then you may need to apply
`brew tap homebrew/science`
